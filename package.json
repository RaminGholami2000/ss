{
  "name": "fetch",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch db.json"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "json-server": "^0.17.4"
  }
}
